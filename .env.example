# Server Configuration
PORT=5000

# Application Settings
NODE_ENV=production

# Base directory for input/output files (mounted volume)
BASE_DIR=./data

# Processing timeout in milliseconds (default 300000 = 5 minutes)
PROCESS_TIMEOUT_MS=300000
